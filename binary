#include <stdio.h>

int main() {
    int n,rem[50],i=0;
    printf("enter the number which is to be converted into binary:");
    scanf("%d",&n);
   
    while(n>0){
        rem[i]=n%2;
        n=n/2;
        i++;
    }
    for(int j=i-1;j>=0;j--){
        printf("%d",rem[j]);
    }
}
